# _triage / pending categorization

Keywords that haven't been placed into their final feature folder yet. Once placement is clear, move each file into the appropriate folder (e.g. `02-keywords/<XX-function>/<sub-function>/`) and remove the `status: triage` line from its YAML.

Files imported from the 2021 PDF reference carry an `imported_from:` field and an HTML comment marking them as imported boilerplate.
