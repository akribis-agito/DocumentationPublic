# IndexPos/AuxIndexPos

**Condition:**

IndexPos is only used when EncType = 1 (digital incremental encoder) or 4 (SIN/COS encoder).

**Definition:**

IndexPos records the latest position where index was encountered.
