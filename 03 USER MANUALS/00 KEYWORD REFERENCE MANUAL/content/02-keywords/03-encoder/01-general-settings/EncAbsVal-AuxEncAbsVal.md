# EncAbsVal/AuxEncAbsVal

**Condition:**

EncAbsVal is only used when EncType=3 or EncType=6 (absolute encoder).

**Definition:**

EncAbsVal is the raw value of the absolute encoder after bit-masking (EncAbsMB) and the handling of direction control (EncDir).
