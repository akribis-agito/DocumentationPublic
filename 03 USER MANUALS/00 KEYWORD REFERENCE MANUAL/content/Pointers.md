Using drawio plugin for charts, mermaid is not sufficient.
Using latex for equations.
Links (Images, hyperlinks) are using standard markdown links with relative paths.
Drawio files are hidden in the navigation pane.
Tables might exceed the width.
- In general, we try to convert it to vertical or another format
- There is also the option of scroll bars, but it might not work well when exporting.
