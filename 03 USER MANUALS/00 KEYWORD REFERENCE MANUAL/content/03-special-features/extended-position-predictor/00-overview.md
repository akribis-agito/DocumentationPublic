# Extended position predictor (EPP)

<!-- TODO: confirm what "EPP" stands for and revise the folder name and
     description accordingly. The keyword set suggests a digital filter
     defined by numerator/denominator integer + fractional coefficients,
     a filter length, and a configurable model range. -->

A model-based filter applied to position feedback. The keywords in this section configure the filter coefficients (numerator and denominator), filter length, and model range, and report the filter's runtime state.
